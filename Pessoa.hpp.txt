#pragma once

#include <string>
#include <iostream>
#include "Pessoa.hpp"

template<typename Documento> class Pessoa { // template function para comparar dois valores de qualquer tipo e retornar


protected:
    Documento documento;
    std::string nome;


public:
    Pessoa() = delete; // construtor padrão removido
    Pessoa(Documento documento, std::string nome); // construtor customizado
    ~Pessoa(); // destructor
    Documento obterDocumento();
    std::string obterNome();
    
private:
    void verificarTamanhoNome();


};
